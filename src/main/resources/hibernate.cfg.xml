<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://35.199.126.247:5432/usersdb</property>
        <property name="connection.username">Diegonfx</property>
        <property name="connection.password">Perrito1</property>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">2</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup-->
        <property name="hbm2ddl.auto">update</property>

        <!-- DB schema will be updated if needed -->
        <!-- <property name="hbm2ddl.auto">update</property> -->

        <mapping class="persons.Account"/>
        <mapping class="persons.Guarantor"/>
        <mapping class="persons.Landlord"/>
        <mapping class="persons.Tenant"/>
        <mapping class="property.details.Blueprint"/>
        <mapping class="property.details.Deed"/>
        <mapping class="property.structure.specifics.BusinessPremise"/>
        <mapping class="property.structure.specifics.Commerce"/>
        <mapping class="property.structure.specifics.Garage"/>
        <mapping class="property.structure.specifics.LivingPlace"/>
        <mapping class="property.structure.specifics.Office"/>
        <mapping class="property.structure.FunctionalUnit"/>
        <mapping class="property.structure.Property"/>
        <mapping class="rents.Contract"/>
        <mapping class="rents.Expenses"/>
        <mapping class="services.specifics.Electricity"/>
        <mapping class="services.specifics.Gas"/>
        <mapping class="services.specifics.Telephone"/>
        <mapping class="services.specifics.Water"/>
        <mapping class="services.Services"/>
        <mapping class="taxes.Taxes"/>

    </session-factory>
</hibernate-configuration>